# Skill Registry — jnvrportfolio

Auto-generated by `sdd-init`. Refresh with `/skill-registry` after installing/removing skills.

## Project Conventions

| File | Purpose |
|------|---------|
| `~/.claude/CLAUDE.md` | Global user rules (voseo, no AI attribution, no `cat`/`grep`/`find`/`sed`/`ls`, no auto-build, etc.) |
| `README.md` | Project overview (stack, scripts, structure, deployment) |

No project-level `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, or similar found.

## User Skills

| Skill | Trigger | Compact Rule |
|-------|---------|--------------|
| `branch-pr` | Creating PR / opening pull request | Follow issue-first enforcement: link issue, conventional commit, descriptive body. |
| `issue-creation` | Creating GitHub issue / reporting bug / requesting feature | Use issue-first workflow with proper labels and acceptance criteria. |
| `judgment-day` | "judgment day", "review adversarial", "doble review", "juzgar" | Launch two blind judge sub-agents in parallel, synthesize, fix, re-judge until pass or 2 iterations. |
| `skill-creator` | Creating a new skill | Follow Agent Skills spec: frontmatter (name, description, trigger), SKILL.md structure. |
| `skill-registry` | "update skills", "skill registry", "actualizar skills" | Scan skills + project conventions, write `.atl/skill-registry.md` and save to engram. |

`go-testing` is installed but NOT applicable to this project (Angular/TS, not Go).

## Compact Rules (auto-injected by orchestrator)

### Angular / Ionic / TypeScript (project stack)

When touching `src/**/*.ts`, `src/**/*.html`, `src/**/*.scss`:

- **Components**: standalone components only (Angular 19 + Ionic 8 pattern). Use `@Component({ standalone: true, imports: [...] })`.
- **Routing**: lazy-load pages via `loadComponent` in `src/app/app.routes.ts`. Don't add eager imports.
- **Selectors**: components prefix `app-` kebab-case; directives prefix `app` camelCase (enforced by ESLint).
- **Suffixes**: page classes end in `Page`, components in `Component` (ESLint rule `@angular-eslint/component-class-suffix`).
- **Styles**: SCSS only; per-page styles in `*.page.scss`; theme tokens in `src/theme/variables.scss` (`--ion-color-*`, mixins like `fadeInUp`).
- **Quotes**: single quotes in `.ts` (editorconfig).
- **Indent**: 2 spaces (editorconfig).
- **Tests**: Jasmine + Karma. One `.spec.ts` per component/page colocated. `TestBed.createComponent` pattern.
- **Lifecycle cleanup**: clear timeouts/subscriptions in `ngOnDestroy` (see `home.page.spec.ts` pattern).

### PR / Branch (when creating PRs)

Inject `branch-pr` rules. See `~/.claude/skills/branch-pr/SKILL.md`.

### Issue creation (when reporting bugs / requesting features)

Inject `issue-creation` rules. See `~/.claude/skills/issue-creation/SKILL.md`.

## Resolution Notes

- **For SDD sub-agents touching code**: inject the "Angular / Ionic / TypeScript" block above as `## Project Standards (auto-resolved)`.
- **For PR creation tasks**: also inject `branch-pr` rules.
- **For issue creation tasks**: inject `issue-creation` rules.
- **For Go code**: skip `go-testing` — not applicable.
